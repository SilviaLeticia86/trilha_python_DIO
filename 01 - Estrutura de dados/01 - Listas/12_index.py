# utilizamos o index para verificar onde objeto ocorre na primeira vez. Se limita a trazer o indice em que o objeto aparece a primeira vez

linguagens = ["python", "js", "c", "java", "csharp"]

print(linguagens.index("java"))  # 3
print(linguagens.index("python"))  # 0
