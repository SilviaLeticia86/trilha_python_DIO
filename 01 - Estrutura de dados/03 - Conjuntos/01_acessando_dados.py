#acessamos passando a posição do argumento que queremos retornar

numeros = {1, 2, 3, 2}

numeros = list(numeros) # se tivermos valores declarados entre {} precisamos alterá-los para uma lista, pois python não acessa os valores de conjuntos diretamente

print(numeros[0])
