cores = (
    "vermelho",
    "azul",
    "verde",
    "azul",
)

print(cores.count("vermelho"))  # 1
print(cores.count("azul"))  # 2
print(cores.count("verde"))  # 1

# a mesma utilização que fazemos em listas. As tuplas possuem menos métodos que as listas (count, index, len)